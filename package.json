{
  "name": "the-card-huddle",
  "version": "1.0.0",
  "description": "Football trading card eBay recent sales tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:pages": "node scripts/build-landing-pages.js",
    "build:card-index": "node scripts/build-card-index.js",
    "test": "node test/bundle-weight.test.js && node test/worker-status.test.js && node test/news.test.js && node test/photo-signature.test.js && node test/cron-wiring.test.js && node test/parallel-index.test.js && node test/checklist-index.test.js && node test/card-index.test.js && node test/market-index.test.js && node test/market-accuracy.test.js",
    "predeploy": "node scripts/build-landing-pages.js",
    "deploy": "wrangler deploy",
    "cf:dev": "wrangler dev",
    "fingerprint:photos": "node scripts/fingerprint-photos.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "stripe": "^21.0.1"
  },
  "//": "Removed: mongodb (streams polyfill broken on Workers), mammoth (only used in parse-2024-checklist.js dev script — install ad-hoc when needed), nodemailer + compression (Node-only, dynamic-required at runtime). three is a devDependency: only the source for the vendored public/vendor/three.module.js build, not imported at runtime.",
  "optionalDependencies": {
    "compression": "^1.8.1",
    "mammoth": "^1.12.0",
    "nodemailer": "^8.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "sharp": "^0.35.3",
    "three": "^0.160.0",
    "wrangler": "^4.0.0"
  }
}
